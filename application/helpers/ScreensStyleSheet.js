import { StyleSheet } from 'react-native'
import { gray } from './Colors';

const ScreensStyleSheet = StyleSheet.create({
    body: {
        backgroundColor: gray,
        flex: 1
    }
})

export default ScreensStyleSheet